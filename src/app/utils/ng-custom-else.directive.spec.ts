import { NgCustomElseDirective } from './ng-custom-else.directive';

describe('NgCustomElseDirective', () => {
  it('should create an instance', () => {
    const directive = new NgCustomElseDirective();
    expect(directive).toBeTruthy();
  });
});
